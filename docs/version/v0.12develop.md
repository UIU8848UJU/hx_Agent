# 引入ruff来检测错误
```bash
pip install ruff # 下载ruff
pip install -e ".[dev]" # 生成python的虚拟环境
ruff format . # 一键格式化
ruff check . # 来检查错误
```

# 增加这些功能
包含“陪伴学习”和“自动整理”。

# 网上cli的核心

# 现在
已直接落地了一版 v0.2 骨架，包含“陪伴学习”和“自动整理”。

**已完成**
1. 扩展数据库 schema（study + organize）
- 新增 `study_sessions / study_messages / study_notes / organize_runs`  
见 [schema.sql](E:\Project\aigent\hx_Agent\hx_agent\index\sql\schema.sql:48)

2. 扩展数据访问层（可直接落库）
- 新增会话/消息/笔记/整理记录接口
- 加了 `ensure_v2_tables`，即使旧库未重建也能自动补表  
见 [meta_store.py](E:\Project\aigent\hx_Agent\hx_agent\index\meta_store.py:217)

3. 新增学习会话服务层
- `start_session` / `study_ask` / `end_session` / `list_notes`
- `study_ask` 流程：检索 -> 生成回答（当前优先规则摘要，预留 LLM）-> 自动记笔记  
见 [service.py](E:\Project\aigent\hx_Agent\hx_agent\study\service.py:20)

4. 新增整理服务层
- `organize_target` 支持文件或目录，输出到 `out/organized`，并记录 run  
见 [service.py](E:\Project\aigent\hx_Agent\hx_agent\organize\service.py:28)

5. 新增 LLM 客户端骨架
- `LLMClient.enabled()/answer()`，后续可接真实 provider  
见 [client.py](E:\Project\aigent\hx_Agent\hx_agent\llm\client.py:26)

6. CLI 已接入新命令
- `hx-agent study start`
- `hx-agent study ask "..." --mode summary --topk 8 [--session-id]`
- `hx-agent study end [--session-id]`
- `hx-agent study notes [--session-id]`
- `hx-agent organize <target> [--template sop] [--out out/organized]`  
见 [cli.py](E:\Project\aigent\hx_Agent\hx_agent\cli.py:40)

**兼容性修正**
- 修了 `init-config` 调用签名问题  
见 [cli.py](E:\Project\aigent\hx_Agent\hx_agent\cli.py:50)

**校验结果**
- 已做 AST 语法校验：`hx_agent/*.py` 无语法错误。
- 当前环境缺少 `typer`，所以没法本地直接运行 CLI 导入测试。

**你下一步可直接执行**
1. `pip install -e .`
2. `hx-agent init-db`
4. `hx-agent ingest data`
5. `hx-agent study start --name "同步机制" --source data`
6. `hx-agent study ask "这里讲了什么核心概念？"`
7. `hx-agent study notes`
8. `hx-agent organize data --template sop`